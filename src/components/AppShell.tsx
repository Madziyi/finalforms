import { AlertTriangle, BarChart3, ClipboardPenLine, Database, FileDown, Gauge, Menu, X } from "lucide-react";
import { useState } from "react";
import { NavLink, Outlet } from "react-router-dom";
import { SyncIndicator } from "./SyncIndicator";
const nav=[{to:"/",label:"Form Entry",icon:ClipboardPenLine},{to:"/data",label:"Data Viewing",icon:BarChart3},{to:"/attention",label:"Attention",icon:AlertTriangle},{to:"/exports",label:"Exports",icon:FileDown},{to:"/system",label:"System",icon:Database}];
export function AppShell(){const[open,setOpen]=useState(false);return <div className="app-shell"><aside className={`sidebar ${open?"open":""}`}><div className="brand-row"><div className="brand-icon"><Gauge size={25}/></div><div><div className="brand-name">ECC Checks</div><div className="brand-subtitle">Operator PWA · v1 rebuild</div></div><button className="mobile-close" onClick={()=>setOpen(false)} aria-label="Close navigation"><X/></button></div><nav className="side-nav">{nav.map(({to,label,icon:Icon})=><NavLink key={to} to={to} end={to==="/"} onClick={()=>setOpen(false)} className={({isActive})=>isActive?"nav-item active":"nav-item"}><Icon size={20}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><SyncIndicator/><p>Every acknowledged tablet save is local and crash-safe. Server confirmation is shown separately.</p></div></aside><div className="main-column"><header className="topbar"><button className="mobile-menu" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu/></button><div className="topbar-title">Energy Conversion Centre</div><SyncIndicator/></header><main className="content"><Outlet/></main></div>{open&&<button className="sidebar-backdrop" onClick={()=>setOpen(false)} aria-label="Close navigation"/>}</div>;}
