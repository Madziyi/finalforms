import { ArrowRight, BarChart3, Camera, CheckCircle2, Clock3, History } from "lucide-react";
import { Link } from "react-router-dom";
import type { FormDefinition } from "../types";
export function FormCard({form,child,showNewEntry=true}:{form:FormDefinition;child?:React.ReactNode;showNewEntry?:boolean}){return <div className="form-card"><div className="form-card-top"><div className="form-tags">{form.aiAssisted&&<span className="tag"><Camera size={14}/> AI assisted</span>}{form.schedule==="time-slot"&&<span className="tag"><Clock3 size={14}/> Every 2 hours</span>}{form.schedule==="derived"&&<span className="tag"><CheckCircle2 size={14}/> Server-derived</span>}</div></div><h3>{form.name}</h3><div className="card-actions">{showNewEntry&&form.schedule!=="derived"&&<Link to={`/forms/${form.key}/new`} className="card-link card-link-primary">New/continue entry <ArrowRight size={17}/></Link>}<Link to={`/data/${form.key}`} className="card-link card-link-history">View past entries <History size={17}/></Link><Link to={`/trends/${form.key}`} className="card-link card-link-trends">Trends <BarChart3 size={17}/></Link></div>{child}</div>;}
